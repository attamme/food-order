const Meals = () => {
    return (
        <ul id="meals">
            { 
                'Create list of meals, using fetch data from backend'
            }
        </ul>
    )
}

export default Meals